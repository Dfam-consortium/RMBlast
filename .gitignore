# Rust build output (workspace root and any member crate)
/target/
**/target/

# C/C++ build output: the vendored ALP library and the alp-fit tool are built
# from source by rmstats' build.rs (feature "alp-fit") and alp-fit/Makefile.
*.o
*.a
/rmblast-stats/alp-fit/alp_fit

# Editor/backup files
*~
*.swp
rmblast-stats/fits/*.err

# Maintainer notes, kept local (delete a line here to start tracking that file).
# The user-visible
# parts of DESIGN_NOTES.md live in README.md's "Known differences from NCBI".
how-to-release
DESIGN_NOTES.md

# Local experimental analysis scripts
/tools/

# Release staging produced by `make dist`
/stage/
/rmblast-*.tar.gz
